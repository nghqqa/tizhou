# 更新日志

## 1.0.10 - 2026-09-02

### 更新源自动选择：国内优先

- 检查更新时自动探测网络：cnb.cool 可达且版本不落后于 GitHub → 从国内镜像检查与下载；否则自动回退 GitHub
- 探测带超时（cnb 4s / GitHub 3s），不阻塞主流程；检查结果提示当前使用的更新源

## 1.0.9 - 2026-09-02

### 能力边界收敛：诚实降级、保留原始资料、禁止错误自动发布

- **能力分类**：每份资料标注能力边界——文字题·可自动导入 / 统计图·复杂表格·建议人工抽查 / 图片题·人工审核 / 图形推理图片题·暂不支持自动结构化 / 无法可靠结构化·仅保留原始资料
- **发布门禁**：非「文字题」能力与「配对待确认」的产物默认禁止发布；审核详情提供「我已查看原始页面并确认内容正确」人工确认勾选，确认后解除限制；发布结果明确报告拦下数量与原因
- **图形推理降级**：实验性图片选项绑定默认关闭（feature flag），图推资料默认逐页保留原始页面图片与 OCR 文字进人工审核——资料不丢失、规律与选项不自动判定
- **无题可切降级**：无法可靠结构化的资料保留转换文本节选与诊断警告，不再静默丢弃；训练式教材仍走「模型提炼」建议路径
- 质量展示改为能力语言，OCR 字符置信度不再单独代表质量结论

### 导入质量模型与图形推理图片优先通道

- **质量模型拆分**：告别单一「平均置信度 99%」——质量报告按维度透出：文字置信度、表格区域数与列数一致性置信度、图形区域数、数字异常数（数字流/多小数点/全角数字）、剥离噪声数、题目切分完整率、选项绑定状态；异常只告警不静默修正，无法确认的内容标记人工审核
- **版面块清单**：结构解析产出版面块清单（类型/页码/坐标）随图片归档走缓存，worker 剥离跨页重复的页眉/页脚与版面模型弃置的水印文本（只删「重复出现 + 边缘区域」，正文品牌名/出处不动）
- **图形推理图片优先通道**：以版面聚类题目（题号位置 + 图片垂直位置），三种绑定形态——四图分离按序绑定 A-D（置信度 85%）、题干图 + 末 4 张选项图（70%）、整图版式四选项见图（55%，人工核对）；图片选项不再因没有文字标签被整批丢弃（实测图推700题：文本通道 20 题弃 19 → 图片通道 55 题，21 组完成绑定，其余带警告进人工审核）
- **图片选项贯通**：选项支持图片字段（text 可为空），产物 markdown/审核页/题库全链路渲染，图片随缓存与发布随行
- **资料分析专项**：解析册数字流行隔离为「对照原图」占位（不混入正文也不静默删除）；「请回答1～5题」组题指引行不再混入材料/题干；HTML 表格一致性检测
- **质量基线报告**：真实样本指标落盘 docs/quality-baseline.md（页面/字符/切题/表格/数字异常/配对率/图片绑定前后对比），样本缺失时自动跳过

### 解析册改道逐页 OCR：修复答案与解析串题

- **解析删除线误划修复**：OCR 把 ≈ 与数值区间（如 1~5题）识别成半角 `~`，而 GFM 单/双波浪线都是删除线语法——任意两个波浪线之间的解析被整段划掉。统一替换为全角～（中文区间标准写法），显示恢复且读感不变
- **「全篇答案」汇总行剥离**：解析册每套末尾印的「【全篇答案】CBADD」不再扫进最后一题的解析

- **问题**：结构解析的文本重排不利于解析册（纯文本流）——相邻题块被挤压进同一块，出现「答案字段是 A、解析结尾却是隔壁题的答案为 B」的串题现象，配对校验同步失效（实测 0/10 通过）
- **修复**：题本保留结构解析（表格/图片保真必需），解析册/答案册自动改道逐页 OCR 文本切块——文件名含「解析/答案」的直接走 OCR；文件名无提示的按参考答案标记密度在转换后识别改道（结果按 OCR 键缓存，一次付费）
- 实测逐页 OCR 时代解析册切块配对校验 30/30 通过；改道后该指标恢复

### 组题连续：资料分析一组材料 + 5 道小题不再被打散

- 直导管线为同套小题生成组标识（groupId + 组内序号），随产物落库
- 练习「随机抽题」「自适应选题」与模拟考试组卷以**组为原子单位**：整组抽取、组内按小题序连续排列，不再被随机打散；放不下的大组先跳过、由小组回填空位
- 材料仍随每题完整保存：错题复习、单题访问时材料/图表/表格不受影响
- 数据库自动迁移新增组字段；已导入的题目重新直导一次即可获得组字段（转换缓存命中，秒级）

### 资料分析图片保真修复：结构解析模式真正跑通

- **调错脚本修复**：结构解析此前误调用 MarkItDown 转换脚本（不认识 `--structured` 参数），导致应用内结构解析从未成功、每次静默回退逐页 OCR——图表被拆成「□城镇 0.97 6 0.87」式文字碎片。现改用 OCR worker 的结构模式，表格还原为 Markdown 表格、图表裁剪为图片随题保存
- **毒缓存自愈**：上述失败期间，OCR 兜底结果曾被错存进结构解析缓存键，导致后续导入永远秒命中纯文本旧结果。现在带错标记的条目视为未命中并自动清除；兜底结果也改按 OCR 键缓存，不再污染结构解析键
- **图片展示贯通**：内置托管题库可读取本地图片附件（此前一票拒绝）；审核页产物与发布后练习页的图表均可渲染
- **图片引用与文件对齐**：RapidDoc 将图片写入深层 `源名/auto/images/` 目录而 markdown 引用是 `images/`——worker 现将图片收拢到引用位置（实测 36 页 32 张图全部对齐）
- **结构解析缓存随行图片**：缓存条目连同图片归档一起存取，命中时恢复到任务目录；缺图片归档或带错标记的旧条目自动清除重转
- **结构化 markdown 容错**：`# 练习题N套` 形式的套标题照常开套且不再混入材料区块；配对校验通过数不再显示负数
- **HTML 统计表渲染**：结构解析还原的表格带合并单元格（rowspan/colspan），GFM 管道表格表达不了——审核页/练习页现以白名单方式渲染 HTML 表格（rehype-raw + rehype-sanitize，拦截导入文件中的脚本等恶意标记），并补齐表格样式
- 文件行质量摘要区分「结构解析 N 页 · 表格还原 · 图片保真」与逐页 OCR，缓存命中提示明示「结构解析结果已复用」

### 直导题库材料与配对

- **套内共享材料捕获**：资料分析每套的统计表/文字资料随题保存，审核页与练习页显示「材料」区块（实测题本 16-18：54 题中 50 题带材料）
- **顺序配对兜底**：题本与解析册套号漂移导致钥匙配对中止时，按文档顺序套内位置配对 + 题干相似度逐对验证（相似度不足保守剔除，宁缺毋错）
- 顺序配对产物透传材料区块（曾漏传导致兜底路径产出全部缺材料）

## 1.0.8 - 2026-08-27

### 直导切题适配：花生十三系资料分析/数量关系

- **数字题干/数字选项识别修复**：题号正则此前会拒绝数字开头的题干（如「6. 2021年全国…」被当小数跳过）、选项拆分拒绝数字开头的选项（如「A. 0.07万亿元」）——资料分析的题干与选项大量以数字开头，导致此前切出 0 题。修复后实测：题本（16-18）54 题全部含完整选项，题本篇 454 题中 208 道数字开头题干成功找回

- **书首目录过滤**：整页「练习题01~30套」目录不再污染套号状态机（实测解析册 392 页全程错位的问题根除）
- **解析册跳号容错**：题干行被吞后，后续题号按真实编号落位而非整套失配（≤5 前进幅度内有效）
- **答案标记变体**：支持【参考答案及正确率】C，89% 格式，正确率接入难度映射
- 实测资料分析600题：配对成功率从 0 提升至 77%（454 题中 350 题答案齐备）；该系列扫描版图表噪声重，残余缺口属 OCR 分段精度范畴

### 结构解析模式（资料分析/图形推理导入管线）

- **引擎第五组件 RapidDoc**：版面检测 + 表格结构识别 + 图形区域裁剪 + 阅读顺序，安装流自动包含
- **直导路由**：PDF 自动走结构解析模式，材料统计表还原为 Markdown 表格、图表/图形裁剪为图片随题入库（文件消息显示「结构解析中」）
- **发布随行资产**：图片随题目发布写入题库 assets 目录
- 实测题本（16-18）18 页：47 秒完成、5 个表格结构化、27 张图形保真；选项行拆分与覆盖率持续迭代中

### 识别精度警示

- **资料分析等图表密集型内容精度标注**：工坊导入页、用户手册与 README 明确——图表密集型内容 OCR 无法保证数字层级关系还原，发布前需重点核对材料数字；置信度只衡量认字把握，不衡量表格结构还原能力

## 1.0.7 - 2026-08-27

### 界面修正

- **直导产物置信度展示诚实化**：规则切题的产物不再显示「置信度 100%」（直导无模型参与，该数字是占位），改为「规则切题」标识；模型提炼产物继续显示 AI 自报置信度。此修正发布于 v1.0.6 安装包制作之后，故独立成版

## 1.0.6 - 2026-08-27

### GPU 加速（可选组件）

- **DirectML 后端**：转换环境新增「GPU 加速」可选组件——检测到独立显卡（NVIDIA/AMD/Intel Arc）时可一键启用，扫描式 PDF 的 OCR 识别速度约提升 2-3 倍（RTX 3090 实测 2.8 倍），识别结果与 CPU 逐字节一致
- **装后探针 + 自动回退**：安装完成前自动验证 DirectML 真实生效；显卡驱动不支持时自动回退 CPU 后端并明确提示，绝不留在"装了等于没装"的状态
- **一键移除**：设置页随时恢复 CPU 推理后端；无独立显卡的机器不显示入口，行为与旧版完全一致
- 引擎状态新增加速器字段与运行时消息标识（「GPU 加速中」）

### 界面改进

- **缓存命中标记**：文件列表常驻显示「· 缓存命中」，转换是实跑 OCR 还是复用缓存一眼可辨

### 转换组件安装源自动优选

- 安装/补装/修复转换组件时，对官方 PyPI 与国内镜像（清华 TUNA/阿里云/腾讯云）快速测速，自动选最快的国内源；工坊里可手动钉死，也尊重用户的 `PIP_INDEX_URL` 环境变量
- 不写系统 pip 配置，仅影响应用内的安装命令

## 1.0.5 - 2026-08-27

### 直导模式支持申论主观题教材

- **确定性解析器**：「【训练一】标题」（夸夸刷式）与「训练一：标题」（酷酷刷式）两种版式的申论教材可从扫描 PDF 直导——按章节自动分类、切分资料正文与提问句、识别年份与卷别、过滤目录页点线残渣与推广水印。
- **段落级题干识别**：设问句以完整段落整体采纳（含被版面折断的长设问），不再被行级关键词误伤截断；材料按句末标点拼段，OCR 断字空格（「F银 行」「需求一提 出」类）自动清洗。
- **零材料目录幽灵过滤**：无材料的目录残片单元一律拒收，不再以「标题=题干」形态污染审核列表。
- **无参考答案入库**：这类教材不含答案，题目以空答案形态入库（带"暂无参考答案"警告），练习配合「申论作答」页 AI 批改兜底；常规刷题判分语义不变。
- **通道择优**：申论书混入的伪客观题残渣不再劫持路由；全书无法切题时明确建议改用「模型提炼」。
- **材料字段贯通**：`material` 全链路写入数据库并参与去重签名，修复此前全库材料为空的缺陷。

### 跨任务转换缓存

- **内容哈希缓存**：转换结果按源文件 SHA-256 + 转换器版本缓存于本地（容量 1 GB LRU 淘汰）。取消后重跑、切换导入模式重试同一批文件时不再重复 OCR——此前每次重导都要从零支付 OCR 时间。
- **PDF 提速**：OCR 组件可用时跳过对 PDF 多余的 MarkItDown 首跑（其结果必然被逐页混合处理的 OCR worker 覆盖）。

### 界面

- **品牌图标**：应用图标替换为朱砂方印「题」（大尺寸带边栏完整印章、小尺寸简化版），告别 Electron 默认原子图标。
- **申论作答页宽屏双栏**：≥1280px 时左栏题目+材料、右栏作答+解析，材料高度放宽；窄屏自动回退单栏。
- **选题下拉优雅截断**：题目预览在句读处收尾并补省略号，不再于词语中间拦腰截断。

## 1.0.4 - 2026-08-26

### 品牌统一收尾：移除旧品牌兼容层

- **移除旧数据目录自动迁移**（旧 `lizhi-kaogong-workbench` → `tizhou`）：经确认无存量老用户，删除更名过渡期的兼容代码。如确有旧品牌安装的数据需要找回，手动把 `%APPDATA%\lizhi-kaogong-workbench` 整个目录改名为 `tizhou` 即可。
- **移除旧版迁移包 App ID 识别**：跨机迁移包导入仅接受题舟（`tizhou`）落款的 manifest。
- 测试临时目录前缀统一为 `tizhou-`，源码、测试、工具、文档全库不再含旧品牌标识（CHANGELOG 历史记录除外）。

## 1.0.3 - 2026-08-26

### 学习报告导出（连接 Obsidian 工作流）

- **导出到 Obsidian**：学习报告页新增主按钮——配置 Vault 后，报告以 Markdown 写入 Vault 的「学习报告」文件夹（文件名含日期与范围），在 Obsidian 中自动出现、可检索可双链；frontmatter 带 title/date/range/tags 属性。
- **导出 Markdown**：原 JSON 导出替换为人可读的 Markdown（保存对话框自选位置），包含核心统计、科目掌握（按正确率降序）、错因分布、每日明细。
- **覆盖保护**：重复导出同日同范围报告自动更新，但只覆盖带题舟生成标记（frontmatter `tz-source`）的文件，同名手写笔记会被拒绝覆盖。
- 新增 `report-markdown.ts` 纯函数模块与 8 项单测（总计 91 项全过）。
- 实机端到端验证：题舟导出 → Vault 落盘 → Obsidian 实时索引并渲染（属性面板 + 四个表格正常）。

## 1.0.2 - 2026-08-26

### Obsidian 检测修复（Issue #1）

- **检测范围扩展**：除官方安装器默认目录外，新增 Microsoft Store 版执行别名、`Program Files (x86)` 全机安装、Scoop 安装目录；所有候选未命中时回退查询注册表 Uninstall 键的自定义安装目录。
- **新增「重新检测」按钮**：学习环境页的 Obsidian 连接区支持手动刷新检测状态——安装或移动 Obsidian 后无需重启应用。
- 检测逻辑抽取为独立模块 `obsidian-detect.ts`（不依赖 electron，可单测），新增 7 项单测覆盖各安装变体与优先级。
- 实机验证：winget 安装 Obsidian 1.13.7 后，学习环境页正确显示已检测（等待 Vault 路径），可执行文件路径自动填入。

## 1.0.1 - 2026-08-26

- 修复 1.0.0 打包版启动即退出：主进程对 electron-updater 的 ESM 具名导入在打包后解析失败，改为 `createRequire` 按需加载（源码与打包环境均兼容）。

## 1.0.0 - 2026-08-26

### 界面「新中式案牍」重设计

- 宣纸白 / 墨黑双主题：四级暗色表面分层与文字对比度规范，滚动条、选区、焦点环全主题化。
- 三级字体系统：界面无衬线、展示衬线、等宽代码各司其职。
- 侧栏印章式品牌标识（朱砂方印「题」），导航分组与激活态重排。

### 今日工作台

- 今日任务对照每日目标、关键数字统计条、14 天训练节奏日历、科目掌握度。
- 薄弱科目自动识别，「补强训练」带参直达专项练习并显示推荐提示。

### 自动更新

- 接入 electron-updater + GitHub Releases：侧栏检查更新 → 确认下载 → 重启安装，仅访问版本信息。

### 发布质量加固

- 打包烟雾测试：`--smoke-test` 启动自检 + `smoke:packaged` 脚本输出到独立 `dist-smoke/`，不污染正式安装包。
- 主进程自动数据目录迁移（旧版 `lizhi-kaogong-workbench` → `tizhou`），老用户升级无感。
- UAT 验证清单 `docs/uat-checklist.md`：12 条关键路径，验证方式如实标注（人工 / 集成 / 单测 / 代码审查）。
- 测试扩充至 76 项。

## 0.9.0 - 2026-08-26

### 学习流程闭环

- **首页补强带入训练参数**：点击"补强/训练"自动传入 mode=adaptive + count + subject，PracticePage 读取并显示推荐提示
- **学习计划可执行**：每个计划项增加"去阅读/去训练/去复习/去变式"跳转按钮
- **模考交卷结果页**：新增 /exam/result/:id，交卷后直接进入，显示得分/正确率/已答/未答/用时，提供错题复习/报告/再考入口
- **exam.get IPC**：支持按 examId 读取单场模考

### 批量审核性能

- **artifacts.reviewMany 批量接口**：531 条产物从 531 次 IPC → 1 次调用，瞬间完成
- **批量结果反馈**：显示"已批准/拒绝 N 项"，部分失败时提示失败数和原因
- **批量操作防重复**：进行中禁用按钮，完成后即时刷新

### 审核状态机

- **四态管理**：pending(拒绝/批准) → rejected(恢复待审核) → approved(撤回批准) → published(只读)
- **恢复待审核**：已拒绝/已批准产物可重置回 pending
- **已发布不可修改**：正确拦截
- **任务状态语义**：全部拒绝后显示"本批已处理"而非"等待审核"

### 审核区布局

- **固定高度内部滚动**：clamp(460px, calc(100vh-260px), 720px)，500 产物不撑高页面
- **分页+筛选**：全部/待审/已批/已拒/有警告，50 条/页
- **自动选中**：进入审核状态自动选中第一条待审核产物

### 按钮系统统一

- **层级规范**：每区域最多 1 个 Primary，Secondary 用于批量操作，Subtle 用于辅助操作
- **文案精简**：动词 2-6 字，数量/说明放按钮外
- **开始导入拆分**：按钮只放"开始导入"，文件数量和说明分离到右侧
- **空选择提示**：0 个文件时显示"尚未选择文件/请先在上方列表选择"

### 表单与布局

- **Select 边框间距**：16px column-gap，边框不贴合
- **三字段垂直对齐**：align-items:start + hint min-height 统一
- **响应式**：900px 双列 → 640px 单列
- **内联样式清理**：全部提取为 CSS 类(builder-start-_/builder-filter-_/builder-pagination)

## 0.8.0 - 2026-08-25

### 品牌重塑

- 产品名从「砺知考公工作台」更名为**「题舟」**（题海行舟，终将上岸）。
- 全量替换：应用名、窗口标题、安装包名、App ID、系统提示词、迁移标识。
- GitHub 仓库从 `kaogong-workbench-x` 更名为 `tizhou`。
- 迁移包兼容旧版创建的包（自动识别旧 app ID）。
- README 重写为独立产品叙事；LICENSE 保留原作者版权并添加新版权行。

## 0.5.0 - 2026-08-25

### 直导质量闸门（防止错误题目污染题库）

- **配对一致性自动校验**：解析册在【参考答案】前重印的题干与题本题干做字符二元组包含度比对（真实 OCR 数据校准：正确配对中位数 0.98、错套配对 0.01，阈值 0.55 下误杀 1.1%、漏网 0%）；整书对齐率过低直接拦截该 book 并提示套号错位，单题不对齐剔除。
- **两段式入库**：直导不再直接落库——切题后进入待审核，抽查（左侧列表右侧预览）后「全部批准」→「发布」两击入库；发布写入目标库「直导题库」目录并重索引，去重照旧幂等。
- **按任务撤销**：已发布的直导任务可一键撤销——删除该任务发布的全部题目文件并重索引，事后兜底。
- 单题纠错：题库为纯 Markdown，可直接改文件后重新索引。

## 0.4.5 - 2026-08-25

- 转换引擎安装改为**分组件安装并显示真实进度**：解析 pip 输出逐包推进，进度条按阶段（创建环境 → MarkItDown → RapidOCR → onnxruntime → pypdfium2 → 验证）显示百分比与当前阶段说明。
- 直导模式反馈前置：开始按钮文案明确“完成后自动入库”，任务运行中持续显示“批次完成后自动配对、去重并写入当前题库”。

## 0.4.4 - 2026-08-25

- 修复侧栏“知识库设置”图标悬停整窗闪屏：移除 Fluent Tooltip 包裹（底部边缘定位触发提示层反复显隐），改标准路由导航并以原生 title 提示；窗口底色对齐新配色。

## 0.4.3 - 2026-08-25

- 修复点击后图片闪屏：react-markdown 的插件/组件映射此前以内联字面量传入，父组件每次重渲染都会整树重建、图片重新走 IPC 加载。现提升为稳定引用，并为题图 data URL 增加内存缓存（重访题目即时显示）。

## 0.4.2 - 2026-08-25

- 题库直导目标改为**当前活动题库**：新题写入 `<题库>/直导题库/` 子目录并原位重索引，不再切换活动库；仅内置示例库时回退应用自管目录。
- 直导自动**去重**：导入前按"题干+材料+首选项"归一化签名比对现有题库与批内已见题，重复跳过并计数——重复导入同一本书变为幂等操作。
- 工坊「产物类型」默认直导题库；直导模式下隐藏只对 AI 模式有意义的"质量流程/自定义整理要求"字段。
- 知识中心空状态页新增"去知识构建导入"直达入口。

## 0.4.1 - 2026-08-25

- 修复 Python 3.14 上 OCR 引擎安装失败：onnxruntime 版本上限（<1.23）导致 3.14 无可选版本，改为开放式下限约束，由 pip 按 Python 版本自动解析。

## 0.4.0 - 2026-08-25（二创维护版首个发布）

基于原项目 0.3.0 的二次开发版本。

### 真题原卷模考

- 题目携带 papers 复现记录（联考共用题去重后仍保留全部试卷归属），161 套真题卷可按原题号顺序整卷组卷、限时 120 分钟，其中 136 套题量完整（≥100 题）。
- 模考页新增“真题原卷模考”入口：选卷即考，题量不足 30 的卷不列出。

### 跨机迁移向导

- 设置页新增“跨机迁移”：导出迁移包（学习数据库安全副本 + 全部用户知识库 + 清单）；新机器导入时自动拷贝知识库、重映射 vault 路径哈希，应用自动重启完成换库。
- API Key 经系统加密不随包携带，导入后需重新填写。

### 界面精修（保守优化）

- 中性色从墨绿调整为暖石墨，与铁锈橙品牌同温，消除撞色浑浊感；明暗双主题同步。
- 做题体验：作答后正确选项绿色、错选红色、其余退隐；选项按钮加大点击区与圆角，题干行距放宽。
- 浏览器表面统一：主题化滚动条、选区色与键盘焦点环；暗色卡片改用顶部高光内衬线表达深度。
- 工坊审核新增“全部批准”批量操作（带确认弹窗）。

### 申论资料库

- 申论训练书（夸夸刷/酷酷刷）提取 48 道作答题（材料+题干），原书未附参考要点，作答后由 AI 评估兜底。
- OCR 文本按句末标点重组中文段落，消除逐行断段造成的随机换行。
- 知识中心保持纯知识点定位：题本类材料只进作答题材料区，不再作为知识文档入库。
- 知识中心切换文档时内容区自动回到顶部；文档布局改为视口内固定高度、列表与内容各自滚动。

### 题库直导入工具

- 知识库工坊新增“直导题库”模式：题本与解析/答案册同批投递，自动分拣、按套号题号配对、合并发布，全程零 API。
- 新增 `src/main/services/question-import.ts` 确定性解析器：题号状态机切题（目录页防污染、OCR 丢号跳切、选项换行归位）、解析册标记块提取、书尾“1-5:BBDBC”分组答案（含重复起止号修正）。
- 工具脚本 `tools/direct-import.mjs`：仓库外批量转换（OpenExam sqlite 种子库、片段阅读600题样例流程），含卷间与跨库去重。

### 知识库工坊：扫描件 OCR

- 新增 `ocr-worker.py`：扫描式 PDF 文本提取不足时自动回退为本地 OCR 识别，逐页汇报进度、可取消。
- OCR 引擎采用 RapidOCR（PP-OCRv6 同源 ONNX 模型，离线可用）与 pypdfium2 渲染；随转换引擎一并安装，已装环境可单独“补装 OCR 组件”。
- OCR 不可用时，失败信息会明确提示补装组件。

### 知识提取吞吐

- 分块从 12000 字细化为 3000 字、每块条数上限从 6 提到 10，并要求片段内完整题目全部提取；修复高密度题本（约 300 字/题）仅能提取约两成题目的问题。单文件总处理量仍为 72 万字。

### 模型连接兼容性

- 放行 198.18.0.0/15 网段，兼容 Clash 等代理 fake-ip 模式下的域名解析安全校验（真实内网网段仍保持拦截）。
- 对 DeepSeek 端点按官方参数关闭思考模式，避免推理占用输出预算导致“模型返回了空内容”或 JSON 截断。
- 知识整理任务最大输出下限提升至 16384 token；输出被截断时给出明确提示。
- 模型 JSON 被截断时自动补齐未闭合括号，抢救已完整的前序条目，避免整块内容作废。
- Windows Python 探测增加裸 `py` 候选，兼容 uv 等工具管理、仅以 `-V:` 标签注册的 Python。

## 0.3.0 - 2026-08-18

首个公开版本。

### 核心能力

- 本地 Markdown 题库与知识文档索引、检索和多 Vault 管理。
- 行测训练、错题复习、模考、申论草稿与学习报告。
- 可选 AI 助教、错题讲解、学习诊断、申论评估和双阶段变式训练。
- 知识库工坊：Microsoft MarkItDown 本地转换、可配置 LLM 提取、二次审校、人工批准和发布。
- 模块化 Prompt 协议与不可信数据边界。
- 模型凭据使用 Electron `safeStorage` 加密保存。

### 已知限制

- 扫描式 PDF 由内置 OCR 组件自动识别；图片文件直接导入尚未支持。
- 视频转写尚未实现。
- Windows 安装包尚未使用商业代码签名。

### 安全

- 开源发布前升级到 Vite 7.3.6，修复开发服务器路径读取类安全公告；官方 npm 审计为 0 个已知漏洞。
